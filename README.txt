how to run the code:

    main.py [env_name]

    env_name can be:
        mc -> MountainCarContinuous-v0
        pd -> Pendulum-v0
        ll -> LunarLanderContinuous-v2

which lib to install:
    gym, numpy, torch, matplotlib